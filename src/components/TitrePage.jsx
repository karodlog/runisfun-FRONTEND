import React from 'react';

const TitrePage = (props) => {
    return (
        <>
            <h2 style={{textAlign: "center", marginTop: "50px"}}>{props.txt}</h2>
        </>
    );
};

export default TitrePage;